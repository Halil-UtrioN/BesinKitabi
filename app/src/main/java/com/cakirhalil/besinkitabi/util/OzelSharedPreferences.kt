package com.cakirhalil.besinkitabi.util

import android.content.Context
import android.content.SharedPreferences
import com.cakirhalil.besinkitabi.roomdb.BesinDB
//import com.cakirhalil.besinkitabi.roomdb.BesinDB.Companion.databaseOlustur
import java.util.prefs.Preferences

class OzelSharedPreferences {
/*
    companion object {
        // SharedPrefrences

        private val TIME = "time"
        private var sharedPreferences: SharedPreferences? = null

        @Volatile
        private var instance: OzelSharedPreferences? = null
        private val lock = Any()

        operator fun invoke(context: Context) = instance ?: synchronized(lock) {
            instance ?: databaseOlustur(context).also {
                instance = it
            }
        }

        private fun ozelShaderPrefrerencesOlustur(context: Context): OzelSharedPreferences {
            sharedPreferences = androidx.prefernce.PreferenceManager
        }
    }*/
}